const Layout = ({ children }) => {
  return (
    <div className="min-h-screen bg-gradient-to-br from-gray-900 via-blue-800 to-purple-800 text-white">
      {children}
    </div>
  )
}

export default Layout
